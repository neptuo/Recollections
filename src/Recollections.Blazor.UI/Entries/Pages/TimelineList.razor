@page "/"
@inherits UserStateAuthenticatedComponentBase
@inject Entries.Api Api

<DocumentTitle Value="Timeline" />

<Timeline DataGetter="@(offset => Api.GetTimelineListAsync(offset))" ShowYearSeparators="true">
    <BeforeContent>
        <EntryCreate />
    </BeforeContent>
</Timeline>