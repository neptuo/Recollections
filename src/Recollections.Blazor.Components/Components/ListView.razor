@typeparam T

@if (IsLoading)
{
    <span>Loading...</span>
}
else if (Items.Count == 0)
{
    <Alert Mode="@AlertMode.Warning" Message="@EmptyMessage" />
}
else
{
    foreach (var item in Items)
    {
        @ChildContent(item);
    }
}