@if (ErrorMessages.Count > 0)
{
    <Alert Title="Validation:" Messages="ErrorMessages" Mode="AlertMode.Error" />
}

@functions
{
    [Parameter]
    public List<string> ErrorMessages { get; set; }
}