name: Client Deploy

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to deploy, e.g. blazor-v0.17.0'
        required: true

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Download release artifact
        run: |
          mkdir dist
          gh release download "${{ inputs.tag }}" --pattern 'recollections-client-*.zip' --dir .
          zip_count=$(ls -1 recollections-client-*.zip 2>/dev/null | wc -l)
          if [ "$zip_count" -ne 1 ]; then
            echo "Error: Expected exactly one client zip artifact, found $zip_count"
            exit 1
          fi
          unzip recollections-client-*.zip -d dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GH_REPO: ${{ github.repository }}

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
